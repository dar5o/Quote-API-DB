// Write tests for Admin reading, updating, and deleting user profiles, as well as error handling for access/permissions (user trying to get list of users), and for admin trying to update/delete a user profile that doesn't exist.